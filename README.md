# PHP coding environment

## Notes

In **Settings > PHP -> Interpreter** map the project root to `/application` even if docker-compose mapping is applied. For some reason, tests don't run if you don't do that.

